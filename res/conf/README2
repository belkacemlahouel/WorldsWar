ENVIRONMENT (WIDTH, HEIGHT)

PORTAL (aENV, aPOSX, aPOSY : bENV, bPOSX, bPOSY)

<<<<< DIFF

--- old version

TRIBE (TYPE, ID)

BODY (TYPE, FONCTION, TRIBEID, ENV, POS, [QTY])

--- new version

TRIBE (TYPE, ID)
	BODY (FONCTION, ENV, POS, [QTY])
	BODY (FONCTION, ENV, POS, [QTY])
	BODY (FONCTION, ENV, POS, [QTY])

TRIBE (TYPE, ID)
	BODY (FONCTION, ENV, POS, [QTY])
	BODY (FONCTION, ENV, POS, [QTY])
	BODY (FONCTION, ENV, POS, [QTY])

etc
	
>>>>> DIFF

RESOURCE (TYPE, ENV, POS, QTY)